// *************** SECTION 2 ABOUT ***************

gsap.timeline ({
    scrollTrigger: {
        trigger: ".box",
        start: "center center",
        end: "bottom top",
        makers : true,
        scrub : true,
        pin : true,
    }
})

.from(".text1", { x : innerWidth * 1 })
.from(".text2", { x : innerWidth * -1 })
.from(".text3", { x : innerWidth * 1 })
.from(".photo", {
    y : innerHeight * 1
})

// ******************* TO DO :

// -> IMG FROM LEFT STAY RIGHT